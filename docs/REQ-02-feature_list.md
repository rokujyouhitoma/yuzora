# [REQ-02] 機能一覧 (Feature List) - ゆうぞら (Yuzora)

本ドキュメントは、青空文庫縦書きビューアー「ゆうぞら (Yuzora)」で実装されている、あるいは今後実装予定の全機能を一覧化し、そのステータス、仕様概要、および要求定義（URD）・要件定義（SRD）とのトレーサビリティ（追跡性）を管理する台帳です。

---

## 1. 機能一覧台帳 (Feature Registry)

| 機能ID | 機能名 | 機能概要 | 対象フェーズ | 実装ステータス | 要求定義 (URD) | 要件定義 (SRD) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **FEAT-001** | 事前定義作品の選択 | 吉川英治「宮本武蔵」8作品をマスターデータとしてプリロードし、ウェルカム画面のカードから選択して即座に読書を開始する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース2 | 3.1 |
| **FEAT-002** | 外部ファイル読込・デコード | ローカルの青空文庫テキスト(`.txt`)やXHTML(`.html`, `.xhtml`)ファイルをドラッグ＆ドロップまたはファイル選択ダイアログ経由で読み込む。Shift_JIS (優先) または UTF-8 を自動判別しデコードする。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース1 | 3.2 |
| **FEAT-003** | ルビ表示 (青空文庫記法) | 青空文庫のルビ記号（`｜漢字《かんじ》` および `漢字《かんじ》`）をパースし、HTMLの `<ruby>`・`<rt>` 要素に変換して表示する。 | v1.0 (初期) | **実装済 (v1.0)** | - | 3.3 |
| **FEAT-004** | 改ページ処理 | 本文中の `［＃改ページ］` 注記を検知し、マルチカラムの列を強制的にブレイクして次のページ（見開き）から開始するように配置する。 | v1.0 (初期) | **実装済 (v1.0)** | - | 3.3 |
| **FEAT-005** | 傍点（強調マーク）対応 | 本文中の `［＃「...」に傍点］` などの注記を検知し、傍点表示クラス（`.bouten`）を適用して該当文字の右側にドット（またはセサミ）マークを表示する。 | v1.0 (初期) | **実装済 (v1.0)** | - | 3.3 |
| **FEAT-006** | 縦書き段組み表示 | 日本語の伝統的な縦書き（デフォルト: 右から左 RTL）および現代的な縦書き（左から右 LTR）に対応。モバイルデバイスでは1カラムに強制し、文章の開始位置を確実に上寄せでアライメントする。 | v1.0 (初期) | **実装済 (v1.0)** | ターゲット/制約 | 3.4 / 4.1 |
| **FEAT-007** | ページ送りナビゲーション | 画面端のクリック/タップ、キーボードの左右矢印キー、およびフリック・スワイプジェスチャー（1スワイプ1ページに厳密制限）によるページ遷移。読書進行方向（RTL/LTR）と順方向・逆方向を完全同期する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース5, 7 | 3.4 |
| **FEAT-008** | ヘッダー・フッターUIトグル | 本文領域のタップ/クリック、キーボードの上下矢印キー押下により、画面上下の操作UI（メニューバー）の表示・非表示をトグル制御する。また、操作停止後3秒で自動非表示化する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース7 | 3.4 |
| **FEAT-009** | 最初に戻る (先頭ジャンプ) | ヘッダー内のボタンから一瞬で本の1ページ目（先頭）に戻る。ダブルアローアイコンの向きは読書方向（RTLなら `>>` 、LTRなら `<<` ）に動的に連動する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース6 | 3.4 |
| **FEAT-010** | ページ指定ジャンプ | フッターの現在のページ数表示（例: `5 / 120 ページ`）をクリックするとプロンプトが表示され、ページ番号を直接数値入力して該当ページへジャンプする。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース6 | 3.4 |
| **FEAT-011** | プログレスバー操作 | フッターのプログレスバー上をクリックした進捗率位置へのジャンプ。および丸いつまみ（インジケーター）のドラッグ（スクラブ）による、本をパラパラめくるようなリアルタイムなページ遷移。送り方向に連動して増加（充填）方向が変化する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース6 | 3.4 |
| **FEAT-012** | 配色テーマ切り替え | ユーザー設定に応じた背景色と文字色のセット切り替え。「和紙 (セピア、デフォルト)」「明 (ライト)」「暗 (ダーク)」「漆黒 (ブラック)」の4種類。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース4 | 3.5 |
| **FEAT-013** | 書体（フォント）切り替え | 読書に適した美しい明朝体（Noto Serif JP）およびすっきりしたゴシック体の切り替え。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース4 | 3.5 |
| **FEAT-014** | 表示スタイル微調整 | ユーザー設定から「文字サイズ（4段階）」「行間（3段階）」「文字間（3段階）」を調整する。変更後は読んでいる位置を見失わないように自動的にリフロー（再計算）する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース4 | 3.5 / 4.1 |
| **FEAT-015** | しおり自動保存 | ページ移動時、プログレスバー操作時、ブラウザタブ閉止時やバックグラウンド切り替え時に、現在の読了進捗率（スクロールパーセンテージ）を `localStorage` へ自動保存する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース3 | 3.6 |
| **FEAT-016** | 読書セッション自動復元 | アプリの起動時、直前に読んでいた本のファイル名、タイプ、本文データ、およびしおり進捗を `localStorage` から自動的に読み込み、前回の続きから即時読書を再開する。 | v1.0 (初期) | **実装済 (v1.0)** | ユースケース3 | 3.6 |
| **FEAT-017** | UI表示設定の永続化 | ユーザーが変更したテーマ、フォント、文字サイズ等の表示オプション情報を `localStorage` に保存し、次回起動時にも自動的に適用する。 | v1.0 (初期) | **実装済 (v1.0)** | - | 3.6 |
| **FEAT-018** | レイアウト / 見切れ診断 | 現在の表示ページのスクロール位置のズレ（アライメント）や、ページ左右の仕切り境界線をまたいで見切れている文字を自動検出し、Markdown形式の診断レポートとしてクリップボードへコピーするデバッグ機能。 | v1.1 | **実装済 (v1.1)** | - | 3.7 |
| **FEAT-019** | 目次表示およびジャンプ機能 (TOC) | 青空文庫の見出し注記（大・中・小見出し）を自動検出し、ルビ情報を除去したプレーンテキスト形式で階層的な目次リスト（ドロワー）を生成・表示。項目クリックで見開き（カラム）を考慮した正確なスムーズスクロールジャンプを実行し、見出しへフォーカスを合わせる。 | v1.2 | **実装済 (v1.2)** | ユースケース8 | 3.8 |
| **FEAT-020** | ユーザー操作履歴の Command パターン化とリプレイ | 書籍ロード、ページ移動、表示設定の変更などの論理的な操作を Command オブジェクトとして記録し、最大100世代の履歴スタック（初期ロード保護付きFIFO）を JSON テキストとしてエクスポート/インポートして自動操作リプレイ再現できるデバッグ機能。 | v2.0 | **実装済 (v2.0)** | - | 3.9 |
| **FEAT-021** | 画面遷移状態管理フレームワーク | シーン遷移を抽象化した「Scene」と「SceneDirector」により画面切り替えを一元管理。遷移中の二重遷移防止を保証し、画面ライフサイクル（`enter` / `exit`）に連動してUIイベントリスナー（welcomeListeners / readerListeners）の動的登録・漏れなき削除を完全自動化。 | v2.0 | **実装済 (v2.0)** | - | 3.10 |
| **FEAT-022** | ハッシュルーティングとブラウザ履歴連動 | URLハッシュ（`#/welcome`、`#/reader?book=xxx`、`#/reader?local=xxx`）によるアプリ状態同期。直リンクによる書籍の直接起動、ブラウザの「戻る/進む」履歴キーへの完全追従、リロード時の読書進捗およびしおりの自動復元を実現。 | v2.0 | **実装済 (v2.0)** | ユースケース3, 5 | 3.11 |


---

## 2. トレーサビリティマトリクス (Traceability Matrix)

本システムは、すべての機能がユーザー要求およびシステム要件と紐づき、かつ設計に漏れなく反映されていることを保証しています。

```mermaid
flowchart LR
    URD["要求定義 (URD)<br>REQ-01"] --> FEAT["機能一覧 (Feature List)<br>REQ-02"]
    FEAT --> SRD["要件定義 (SRD)<br>REQ-03"]
    SRD --> HLD["基本設計 (HLD)<br>DSN-01"]
    HLD --> LLD["詳細設計 (LLD)<br>DSN-02"]
```
