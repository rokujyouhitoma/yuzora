---
ID: 016
種別: Refactor
優先度: Medium
ステータス: Draft
---

# [Refactor] サイクロマティック複雑度の計測とリファクタリング基準の導入 (ID: 016)

## 1. 概要 / Summary
コードの保守性と可読性を維持するため、サイクロマティック複雑度（循環的複雑度）を自動的に計測し、定義された閾値を超える関数やモジュールを検出してリファクタリングを促す、あるいはビルド/CIプロセス等で警告・エラーにする仕組みと手法を検討・導入する。

### 検討する手法の候補：
1. **ESLint の `complexity` ルール**:
   - 既存の ESLint 構成にルールを追加し、設定された閾値（例: 10 または 15）を超えた場合のエラー/警告を出力する。
2. **静的解析ツール (例: `escomplex`, `typhonjs-escomplex`)**:
   - 静的解析スクリプトを記述し、より詳細なメトリクス（Maintainability Index など）を収集・レポートする。
3. **CI/CD またはコミット前フック (Husky + lint-staged) での自動チェック**:
   - 閾値以上の複雑度を持つコードがコミット・マージされるのを防ぐ。

これらの中から、本プロジェクト（バニラJSベース）に最適な手法を評価し、実装計画を策定する。
