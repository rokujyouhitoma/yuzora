/**
 * Yuzora - Service Locator Module (Application Bindings)
 */
"use strict";

// Global locator singleton instance using frameworks/locator.js definition
const locator = new Locator();
window['locator'] = locator;
